import{env}from"cloudflare:workers";import{NextResponse}from"next/server";import{getSession}from"../../../auth";import{addBbyExcludedOrders,bbyDatabase,deleteBbyExcludedOrder,listBbyExcludedOrders}from"../../../../lib/bby";
const storeOf=(v:unknown)=>v==="JS"?"JS" as const:"SAP" as const;
export async function GET(request:Request){const session=await getSession();if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});const store=storeOf(new URL(request.url).searchParams.get("store"));const result=await listBbyExcludedOrders(bbyDatabase(env,store),store);return NextResponse.json({store,rows:result.results});}
export async function POST(request:Request){const session=await getSession();if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});try{const body=await request.json() as any,store=storeOf(body.store);return NextResponse.json(await addBbyExcludedOrders(bbyDatabase(env,store),store,body.orderIds||[],body.note||"",session.email));}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Submission failed"},{status:400});}}
export async function DELETE(request:Request){const session=await getSession();if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});try{const body=await request.json() as any,store=storeOf(body.store);if(!body.orderId)throw new Error("Order ID is required");return NextResponse.json(await deleteBbyExcludedOrder(bbyDatabase(env,store),store,body.orderId));}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Delete failed"},{status:400});}}
